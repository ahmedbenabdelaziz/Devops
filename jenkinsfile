pipeline {
    agent any

    stages {
        stage('Checkout SCM') {
            steps {
                checkout scm
            }
        }

        stage('Cleanup') {
            steps {
                cleanWs()
            }
        }

        stage('Checkout Code') {
            steps {
                script {
                    checkout([
                        $class: 'GitSCM',
                        branches: [[name: '*/main']],
                        userRemoteConfigs: [[url: 'https://github.com/ahmedbenabdelaziz/Devops.git']]
                    ])
                }
            }
        }

        stage('Verify Files') {
            steps {
                script {
                    // Afficher les fichiers dans le répertoire pour vérifier si le projet est bien cloné
